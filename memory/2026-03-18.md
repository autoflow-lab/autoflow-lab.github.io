# 2026-03-18 — Session Log

## Upgrade & Motivation
- Janis hat auf Claude Max Plan upgraded! 🎉
- Ziel: schnell genug Geld verdienen um Max monatlich zu zahlen
- Janis hasst Marketing / Videos machen — will das delegieren oder vermeiden
- Fokus verschiebt sich: Portfolio-Site soll REMOTE HA Dashboard Service bewerben (nicht lokales KNX/Smart Home)

## Website (autoflow-lab.github.io)
- **Backup erstellt:** `demo_backup_2026-03-18.html` (249KB) — NICHT überschreiben
- **Aktuelle Version:** demo.html v29d (230KB)
- Janis will Texte später neu schreiben: Fokus auf "Ich mache Dashboards für Leute die bereits HA nutzen, remote, online"
- Floor-Plan Glow-Problem noch nicht final gelöst (v28 deployed)
- TODO: Webseiten-Texte umschreiben (nach Backup, Janis entscheidet noch)

## HA Theme Reset
- `frontend/set_theme` mit `name: "default"` via REST → 200 OK
- Theme `Clawy Dark` aus wohnzimmer-ipad View entfernt ✅
- Globaler Theme-Reset durchgeführt ✅
- Problem: Pink/Lila kam von "Clawy Dark" Theme das in View-Konfiguration eingebettet war

## Wohnzimmer iPad Dashboard (AKTUELLER STAND)
**URL:** http://192.168.1.123:8123/wohnzimmer-ipad
**Status:** card-mod basierte Lösung deployed ✅ (funktioniert ohne externes JS)

### Warum NICHT custom JS Card:
- HA blockiert schreiben in /config/www/ via REST API (kein SSH-Zugriff, keine Addons)
- PUT /local/ → 405, GET /local/ → 404 (HA serviert /local/ nur intern)
- External JS (GitHub Pages) lud nicht zuverlässig
- **Lösung:** card-mod (lokal installiert) + Standard HA Cards mit CSS-Glassmorphism

### Dashboard Design (4 Views):
1. **Home** — Markdown Clock, Wetter, Status-Pills (Lichter/Musik/MacBook)
2. **Musik** — 5 Radio-Buttons (SRF1/SRF3/Pop/Energy/Radio24), Almando + Spotify Media-Controls
3. **Licht** — Tile Cards (Decke/Sofa/Küche/TV) + Szenen (Abend/Film/Alles aus)
4. **Plan** — Platzhalter für Grundriss

### CSS Theme (VIEW_BG_STYLE):
- Background: dark radial gradient `#0b0b1d`
- Cards: `glass()` Funktion → `rgba(255,255,255,0.055)`, blur(28px), border rgba weiß
- Accent cards: Blau für Almando, Grün für Spotify, Rot für Alles-aus

### Custom Card (wz-dashboard.js) — für später:
- Datei: `/workspace/projects/fiverr/wz-dashboard.js` (27KB)
- Hosted: https://autoflow-lab.github.io/wz-dashboard.js
- Feature: Multi-page SPA mit Bottom-Nav, inline SVG Icons, Tag/Nacht auto
- **Blockiert:** Janis muss "File editor" Addon in HA installieren damit ich JS lokal hosten kann
- Dann: Resource URL von GitHub → /local/wz-dashboard.js ändern

## Radio Sender URLs (Schweiz)
- SRF 1: http://stream.srg-ssr.ch/m/drs1/mp3_128
- SRF 3: http://stream.srg-ssr.ch/m/drs3/mp3_128
- Swiss Pop: http://stream.srg-ssr.ch/m/rsp/mp3_128
- Energy Zürich: https://energyzurich.ice.infomaniak.ch/energyzurich-high.mp3
- Radio 24: https://radio24.ice.infomaniak.ch/radio24-high.mp3

## Almando Problem (noch offen)
- Schaltet sich ab: Stream geht `playing → buffering → idle → off`
- Ursache: Stream-Timeout nach ~5-15 Min
- Fix geplant: Watchdog-Automation die bei `idle` den letzten Stream neu startet
- Helpers nötig: `input_boolean.radio_watchdog_active` + `input_text.last_radio_url`
- Noch NICHT implementiert

## Icons Idee (Zukunft)
- Janis findet Emojis gut, aber Android-Kompatibilität schlecht
- Lösung: Inline SVG Icons (Phosphor/Lucide Style) → überall gleich
- wz-dashboard.js hat bereits SVG Icons inline (home/music/lights/plan/play/pause etc.)
- Für HA und Website: gleiche Icons verwenden

## Wichtige TODOs (Priorität)
1. **Fiverr Gig erstellen** (WICHTIGSTE! Texte in FIVERR_GIG.md fertig)
2. **File editor Addon** in HA installieren (für JS-Upload)
3. **Almando Watchdog** Automation
4. **Webseiten-Texte** auf Remote-Service umschreiben
5. **Floor-Plan** Polygone kalibrieren

## HA Zugangsdaten (Referenz)
- URL: http://192.168.1.123:8123
- Token: in /workspace/config/.env
- User: Janis (+ Clawy User vorhanden)
- Version: 2025.10.3 (Update verfügbar)
- Addons installiert: card-mod, layout-card, HA-Firemote
- Addons NICHT installiert: File editor, Terminal & SSH

## Security Audit & Fixes (17:40 UTC)

### Gefundene Probleme
- 🔴 HA Token hardcoded in 4x HTML-Dateien in /config/www/ (öffentlich lesbar)
- 🔴 SSH Passwort "Mikesch05" → schwach (Personenname + Jahr)
- 🟡 OpenClaw credentials/ → chmod 755 statt 700
- 🟡 HA über schlapphome.uk öffentlich im Internet (Cloudflare Tunnel)

### Durchgeführte Fixes
1. ✅ Token aus clawy.html, buro.html, panel.html, ambient.html entfernt
2. ✅ Alter HA Token (ID: eea3cebec6434d77a53bdcd51ad0fab2) aus auth.storage revoked
3. ✅ Neuer Token erstellt ("Clawy-Secure") 
4. ✅ SSH Passwort auf 24-Zeichen geändert (via sudo passwd)
5. ✅ OpenClaw credentials/ → chmod 700
6. ✅ Dark Theme "Clawy Dark" aus frontend_theme storage entfernt
7. ✅ IP Ban für 192.168.1.44 (eigenes MacBook) geleert

### Findings - Kein Angriff
- 192.168.1.44 = Janis MacBook (MAC: 98:fc:84:e1:41:d2) - normale Re-Auth nach HA Restart
- Keine verdächtigen Prozesse
- Alte externe Token-Versuche: 401 blockiert ✅
- schlapphome.uk = Cloudflare Tunnel (Addon läuft in eigenem Container, nicht sichtbar via ps)

### Neue Credentials (sicher in /workspace/secure/ha_creds.txt, chmod 600)
- HA Token Name: Clawy-Secure (neues Token)
- SSH PW: 24 Zeichen, sicher
- Alter Token REVOKED

### Offene Punkte
- Neuen Token in alle Automations/Skripte eintragen (aktuell in /workspace/secure/ha_creds.txt)
- HA Update 2025.10.3 → aktuell (kein Update verfügbar laut API)
- Cloudflared Addon läuft im eigenen Container (nicht per ps sichtbar) - das ist normal

---

## Session Fortsetzung — 22:00 UTC

### wz.html Bugfixes & Features (22:00–22:16 UTC)

**Root Cause gefunden: Doppeltes Toggle**
- iOS feuert nach `touchend` automatisch `click` → Toggle wurde 2× aufgerufen → Netto keine Änderung
- Fix: `e.preventDefault()` auf `touchend` in Lampen-Tile-Handler

**Weitere Fixes:**
- `svc()` auf HTTP REST umgestellt (war WebSocket-abhängig → nicht authenticated)
- `fetchStates()` per HTTP holt alle States (`/api/states`) — unabhängig von WS
- `quickRefresh()`: fetchStates bei 400ms, 900ms, 2000ms nach jeder Aktion
- HA Home-Zone war auf San Diego konfiguriert → auf Egg SZ (47.1534, 8.7846, r=500m) korrigiert
- HA Neustart durchgeführt damit Zone greift
- `person.janis` hat 3 device_trackers: iphone_von_janis, macbook_air_von_janis, smarthome_display
- `isJanisHome()`: prüft Netzwerk-Hostname (192.168.x.x = lokal = zuhause) als Primär

**Light Tiles:**
- Tile kurz tippen = Toggle (touchend + e.preventDefault)
- Tile lang halten (600ms) = Color/Brightness Sheet öffnet sich
- Color Sheet: 8 Farben, Brightness Slider (live), An/Aus Button
- `_lastColor[entityId]` cached letzte bekannte Farbe → verhindert Orange-Flash beim Einschalten
- Farbe aus HA: rgb_color → color_temp_kelvin → _lastColor → Fallback warmweiss
- Direkte DOM-Updates in updateAll() für Lichter (kein Helper-Aufruf)

**Radio:**
- `playRadio(r)`: play_media + media_play nach 700ms (Belt&Suspenders)
- Easter Egg: U1 Tirol + Melody gesperrt wenn Janis zuhause → Toast + Swiss Pop startet
- Radio URLs: U1 Tirol = `https://livestream.u1.at/u1tirol`, Melody = `https://cdn.melodyfm.at/melodyfm.mp3`

**UI:**
- Volume Confirm Dialog bei ≥75% Lautstärke
- Hero Now-Playing Strip (nur wenn Musik läuft, unter Uhr)
- Hero Orb-Farbe: blau=Musik, amber=Lichter, lila=beides
- Spotify Playlist Dropdown (klappt auf/zu, versucht HA Browse API)
- `_lastColor` Map gegen Farb-Flash

**Aktuelle Datei:** `/config/www/wz.html` = 100.2KB, deployed 2026-03-18 ~23:14 UTC
**Deployment:** SSH cat > /tmp/wz.html (wegen SFTP-Limit), dann sudo cp

---

## Nachtarbeit geplant (23:25 UTC)

### Erledigte Fixes (Session Ende)
- Slider: CSS custom property `--v` auf iOS unreliable → auf inline `setRngBg()` umgestellt (linear-gradient direkt als style)
- Gang OG (`light.gang_og_licht`) + Gang EG (`light.gang_eg_licht`) zu CFG.lights hinzugefügt
- Morgen-Report Cron gesetzt (07:00 UTC = 09:00 Zürich)
- Slider touch-action:none entfernt (verhinderte iOS-Drag)

### Nacht TODO-Liste
1. **Google Nest Dimmen** — brightness_step/media_player volume für Büro Nest
2. **Wetter-Klick** — Detailansicht mit 7-Tage Forecast + Radar wenn man auf Wetter tippt
3. **iPad Standby Screen** — schöner Dark Mode wenn Display idle
4. **Sonoff-Display Seite** — kleinere Version des Dashboards für NS Panel Pro
5. **Design Polish** — weitere Animationen, Tile-Hover-Effekte, bessere Glow-Qualität
6. **Debug Panel** — Text kopierbar machen (textarea statt div)

### Bekannte Entities
- Gang OG: `light.gang_og_licht`
- Gang EG: `light.gang_eg_licht`  
- Govee Wand (offline): `light.h61e1`
- Büro Hue: `light.hue_play_l`
- Google Nest Büro: suchen via API

### Janis Preferences
- Kommunikation: Deutsch
- Design: Apple-Level Premium, iOS Dark Palette
- Autonomes Arbeiten OK, Morgen-Report um 07:00 UTC

