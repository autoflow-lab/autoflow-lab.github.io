
## 2026-03-27 (Nacht-Session Fortsetzung)

### wz.html Debugging-Chaos
- Hatte einen langen Debug-Loop wegen "keine Tabs / komische Touch-Events"
- Root causes identifiziert:
  1. `const _dg=...` Fix war korrekt (dg Element fehlte), aber file wurde bei SSH-Read truncated (4s timeout für 444K File nicht genug) → 357K kaputte Version deployed
  2. Debug-Overlay `z-index:999;inset:0` blockierte alle Taps
  3. `vol-range` Input war `height:100%;opacity:0` → fing alle Touches auf Music-Page ab → Scroll = Lautstärke
  4. Mehrere fehlerhafte Patches aufeinander gestapelt
- **Lösung**: Clean restore von git commit `25e63a9` + minimale sichere Patches + iPad CSS Robustheit

### iPad CSS Fixes deployed (commit 72468dc)
- `height:-webkit-fill-available` Fallback für 100dvh
- `min-height:0` auf .pages
- `display:grid !important` auf nav
- `touch-action:none` auf input[type=range]
- `*{touch-action:manipulation;-webkit-tap-highlight-color:transparent}`
- File: 500,909 bytes deployed

### wz.html aktuelle Features
- Kiss FM hinzugefügt (CFG.radio): `{n:'Kiss FM', c:'#e11d48', r:'225,29,72', u:'http://stream.kissfm.de/kissfm/mp3-128/internetradio/'}`
- dg null-fix: `const _dg=getElementById('dev-cards')||getElementById('dg');if(_dg)...`

### family.html v4.3 deployed
- Spotify Section für Janis + Sidonia (play_media auf media_player.spotify_janisss)
- Kurt: Kiss FM + SRF3 Radio-Favs hinzugefügt
- Profil-System (Janis/Kurt/Brigitte/Sidonia) via URL `?wer=kurt`

### SSH Read Timeout Problem (WICHTIG!)
- Bei grossen HTML-Files (>400KB): `time.sleep(4)` nach exec_command NICHT GENUG
- Mindestens `time.sleep(10-15)` verwenden, oder besser: channel.recv_exit_status() abwarten
- Truncation führt zu kaputten Deploys ohne JS-Fehler!

### Yuki / Instagram Automatisierung
- User will alles automatisieren (Bild-Gen, Posting, DM-Replies)
- Instagram Graph API (offiziell, Business Account) als legitimer Weg besprochen
- Benötigt: Instagram Business Account, Facebook Page, Developer App Token
- Image Gen: Replicate (Flux) ~€0.003/Bild oder DALL-E 3 ~€0.04/Bild
- Midjourney läuft über Discord → ich kann Discord-Commands senden
- User erwähnte "nano banano wavespeed" = Video-to-Video AI Workflow (Wan 2.1 + WaveSpeed)
- LoRA-Training für Yuki-Konsistenz empfohlen

### Profil-System Architektur
- Alle 4 Profile in einer einzigen family.html (URL-Param ?wer=)
- LIGHT_ROOMS, LIGHT_COLORS etc. alle geteilt → Änderungen propagieren automatisch
- Pro Profil: roomOrder, hideRooms, roomAliases, showRadio, dimmerRooms
- Kurt/Brigitte: Eltern SZ umbenannt zu "Schlafzimmer"

### Raum-Dimmer Bug fix (v4.2)
- _S State-Check Timing-Problem: Fallback auf alle dimmbare Lichter wenn keine als 'on' erkannt
- Slider-Wert wird nach 1.5s nochmal synchronisiert (nach WS-Init)
- Dimmer sync auch bei jedem updateLights() Cycle

