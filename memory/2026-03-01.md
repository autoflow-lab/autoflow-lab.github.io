# 2026-03-01 — Sonntag

## Erledigte Aufgaben

### Radio Scripts
- Radio-Scripts (`srf1`, `swisspop`, `u1_tirol`, `radio_central`, `radio_melody`, `radio_eviva`) neu in `/config/scripts.yaml` erstellt — alle spielen auf Almando (`media_player.wohnzimmer_alm`) mit Lautstärke 0.25
- Alle Scripts waren `unavailable` (gelöscht aber im Cache), komplett neu erstellt

### LED Automationen (Govee)
- `govee_tv_an_dunkel` — TV an + Sonne unter Horizont → Wand-LED (`light.h61e1`) an mit warm-amber
- `govee_knopf_toggle` — Shelly Input 3 (unten rechts am Wandschalter) togglet alle 3 LEDs
  - Nur Wand an → alle 3 an; Alle an → alle aus; Alle aus → alle 3 an
  - Debounce 200ms fix wegen Blinken
- `govee_leds_aus_wenn_tv_aus` — 2 Min nach TV aus → alle 3 LEDs aus
- LED Entitäten: `light.h61e1` (Wand), `light.h618a` (Sofa), `light.h618a_2` (Küche)
- Govee unterstützt RGB — Farbe noch offen bis Janis Govee-App Farbe wählt

### Google Nest Hub Dashboard (Büro)
- `sensor.time` + `sensor.date` via `time_date` platform zu `configuration.yaml` hinzugefügt + HA Restart
- Lovelace Dashboard `clawy-dashboard` → neue View `buro` mit `picture-elements` auf dunklem Hintergrundbild
- Hintergrundbild `/config/www/buro_bg.png` generiert (dark gradient mit lila Akzent)
- Buttons: `type: icon` mit `tap_action` (nicht `service-button` — funktioniert nicht im Cast)
- Icons: `mdi:book-open-variant` (Arbeiten), `mdi:coffee` (Entspannen), `mdi:power` (Aus), `mdi:spotify` (grün, Mood)
- Cast via `cast/show_lovelace_view` auf `media_player.nest`
- `type: label` funktioniert NICHT im Cast (Konfigurationsfehler)
- `type: icon` + `tap_action` funktioniert ✅
- Licht funktioniert, Spotify noch nicht perfekt (Nest kann nur eins gleichzeitig — HA Cast ODER Spotify)
- Spotify Mood Button jetzt via `script.spotify_mood` → wählt MacBook als Source

### Sunrise TV Cast
- SRF Streams haben DRM — kein Cast möglich auf LG TV Browser
- YouTube Highlights (`Q7MWpHIdbiU` — Schweizer Festspiele Kandahar) als Fallback gezeigt
- SRF Resultcenter Seite als iframe versucht — funktioniert nicht gut

### TV Dashboard (LG)
- `/config/www/tv.html` + Lokaldatei `projects/clawy_tv.html` weiterhin aktiv
- URL: `http://192.168.1.123:8123/local/tv.html`

### Ambient TV Dashboard (Büro-TV am Laptop)
- Neues Dashboard `ambient.html` für den TV der am Laptop angeschlossen ist
- URL: `http://192.168.1.123:8123/local/ambient.html`
- Lokaldatei: `projects/ambient_tv.html`
- Features: Uhrzeit mit Neon-Glow, Wetter+Forecast zusammen, Spotify widget, Märkte (Yahoo Finance), Crypto (CoinGecko), Rotating Widget (Tips/Quotes/Facts/HA Status), Sonnenauf/-untergang Bar, News-Ticker (SRF), Smart Home Status
- Sanfte Farbübergänge ~20s: Pink → Cyan → Grün → Orange → Lila → Rot
- Neon-Farben für Up/Down: `#39ff14` / `#ff073a`
- Stocks werden server-seitig vorgebaut + live refresh via Yahoo Finance
- News server-seitig vorgebaut (SRF RSS) + live via allorigins.win

### OpenRouter
- API Key eingerichtet: `sk-or-v1-bc49...` (gespeichert via `OPENROUTER_API_KEY` env in openclaw.json)
- Claude bleibt primär, OpenRouter/Gemini als potenzielle Alternative
- Modelle verfügbar via `openrouter/...` prefix

## Offene Punkte / Nächste Schritte
- Spotify auf Nest Hub: Nest kann nicht gleichzeitig HA Cast + Spotify — Lösung ausstehend
- Govee LED Farbe: Janis wählt Farbe via Govee App → dann RGB-Wert auslesen und als Standard speichern
- Wecker Automation: `clawy_wecker_janis` noch auf falsche Zeit (09:00, soll 06:25 Mo–Fr)
- Redundanter Wecker Cron `ffa41356-0a03-4cef-916f-49a8a497aa97` noch aktiv
- iPhone Shortcut für Sleep Cycle → HA noch nicht von Janis erstellt
- Music Assistant HACS Integration noch nicht installiert
- Fanvue/Instagram Accounts noch nicht erstellt
- Image Generation für Yuki noch nicht verfügbar

## Technische Erkenntnisse
- Nest Hub Cast: custom cards (`layout-card`, `custom:html-template-card`) laden NICHT im Cast-Modus
- Nest Hub Cast: `type: label` in picture-elements → Konfigurationsfehler
- Nest Hub Cast: `type: icon` + `tap_action` → funktioniert ✅
- Nest Hub Cast: `background` CSS auf View → Crash im Cast
- Nest Hub Cast: iframe-Karten → weisser Screen (JS blockiert oder CSP)
- `service-button` in picture-elements funktioniert mit alter Syntax, aber `type: icon` mit `tap_action` ist besser in 2025.10
- HA `time_date` Sensor: `sensor.time` (HH:MM), `sensor.date` (YYYY-MM-DD)

## Sunrise/Sunset Daten heute
- Egg SZ: Sonnenaufgang ~07:xx, Sonnenuntergang ~18:xx (je nach Wetter-API)

## Abend-Session (21:00–22:00 UTC)

### Erledigtes
- Govee Button Fix: `for: milliseconds: 200` entfernt → Button reagiert jetzt sofort
- Büro Shelly+Hue Sync: Neue Automation `clawy_buro_shelly_hue_sync`
- Bad Musik: Direkter MP3-Stream statt media-source → schnellerer Start
- Multiroom WZ Automation entfernt (Janis will das nicht)
- Gäste WC Automation neu: Wenn Almando spielt → Nest Mini WC übernimmt denselben Stream; sonst Swiss Pop Fallback
- Wecker Multiroom: Nur wenn Janis zuhause (person.janis=home) + Spotify Playlist statt Swiss Pop
- Template Lights erstellt: light.garten_licht, light.gaste_wc_licht, light.gang_eg_licht, light.gang_og_licht
- Wecker Zeit 09:00 → 06:25 Mo–Fr + Bedingung person.janis=home
- Lovelace Lichter-Dashboard überarbeitet: nach Räumen sortiert, tile-Karten, "Schalter" Section entfernt
- Räume-View neu erstellt im Dashboard
- Deckenlampe korrekt → Schlafzimmer (Hue Gerät mit Effekten)
- Kleiderschrank + Eltern SZ aus Dashboard entfernt
- LED Bett entfernt (Eltern SZ)
- Gäste WC → "Bad" umbenannt
- input_text.clawy_notification erstellt für Screen-Notifications
- Ambient Dashboard: Notification Overlay eingebaut (Clawy kann direkt auf Screen schreiben)

### Raumzuordnung (bestätigt)
- Büro: light.buro (Hue), light.hue_play_l, switch.shelly1_98cdac0ca9b2 (Wandlampe)
- Wohnzimmer: light.deckenlampe (Hue), light.h61e1 (Wand LED), light.h618a (Sofa), light.h618a_2 (Küche)
- Schlafzimmer (Janis): light.schlafzimmer, light.hue_iris, light.deckenlampe (Hue mit Effekten)
- Wait: deckenlampe ist im Schlafzimmer ODER Wohnzimmer? → Janis bestätigt: Schlafzimmer
- Eltern SZ: light.eltern_schlafzimmer, light.color_temperature_light_1 (LED Bett) → NICHT anfassen!
- Gänge: light.gang_eg_licht, light.gang_og_licht
- Garten: light.garten_licht
- Bad (Gäste WC): light.gaste_wc_licht, media_player.nestmini1153

### Offene Todos
- Bad Musik Delay live testen mit Janis
- Spotify synchron im Bad beim Wecker → nur annähernd möglich (Spotify Connect Gruppe)
- Govee Wand LED (light.h61e1) war unavailable am Abend → Stecker Reset nötig
