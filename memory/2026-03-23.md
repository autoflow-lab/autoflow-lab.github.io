
## Nacht-Improvement Run 01:00 UTC

Zwei Fixes in wz.html implementiert + deployed:

1. **Light Mode Fix**: `applyTheme()` rief `updateHeroGradient()` nicht auf → beim Theme-Wechsel blieb der dunkle Hero-Gradient aktiv. Jetzt wird der Gradient sofort bei jedem Theme-Wechsel neu gezeichnet.

2. **Timer-Funktion Lichter**: `⏱`-Button im Licht-Detail-Sheet. Cycling: 5→10→15→30→60 Min → Abbruch. Timer läuft im `window._lightTimers` Objekt, überlebt Sheet-Schließen. Live-Countdown im Tooltip.

Deploy: SSH (via sudo + gzip+b64 Pipe) → /config/www/wz.html ✅
Commit: 0a3d47c

## Security Audit 2026-03-23 07:xx UTC

### Gefundene Issues und Fixes:

**✅ GEFIXT:**
- ha.apk (36MB APK) war öffentlich unter schlapphome.uk/local/ha.apk → gelöscht
- ipad.html: hardcoded 192.168.1.123 in GitHub Pages → gefixt + gepusht
- nsscreen.html: hardcoded http://192.168.1.123:8123 → relative URL
- nest-hub.html: hardcoded http://192.168.1.123:8123 → relative URL
- HA CORS: https://google.com entfernt (unnötig)
- GitHub Pages ipad.html: private IP entfernt

**⚠️ OFFEN / AKZEPTIERT:**
- SSH PasswordAuthentication yes auf Port 22 → empfohlen: Key-Only, aber Janis nutzt Passwort
- /local/ files öffentlich ohne Auth (wz.html, family.html etc.) → DESIGN SO GEWOLLT (OAuth inside)
- localStorage Token-Storage → Standard für HA PWA, kein besserer Weg ohne Backend
- family.html: schlapphome.uk hardcoded → nur externe URL, kein Secret

**🔒 BESTÄTIGT SICHER:**
- HA API ohne Auth → 401 (korrekt)
- Extern ohne Auth → 401 (korrekt)  
- Kein JWT/Token in GitHub Pages (ipad.html war IP, kein Token)
- Keine hardcoded Tokens in irgendeiner Datei
- OAuth PKCE Flow in wz.html (korrekt implementiert)

## Session 2026-03-23 07:09 UTC — Pre-Compaction Flush

### Morgend-Session Zusammenfassung

**Security Audit durchgeführt (07:xx UTC):**
- ha.apk (36MB) aus /config/www/ gelöscht — war öffentlich zugänglich
- ipad.html: hardcoded 192.168.1.123 entfernt → GitHub Pages gepusht
- nsscreen.html + nest-hub.html: HTTP-IPs → relative URLs
- HA CORS: google.com entfernt
- Alle APIs korrekt geschützt (401 ohne Token)

**Presence-Fix (gestern Nacht):**
- person.janis nutzte device_tracker.smarthome_display → immer "home"
- Fix: device_tracker.iphone_von_janis_2 (GPS/Mobile App) als Quelle
- In wz.html, family.html, getGreeting() gefixt

**Neue HA Lovelace Dashboards:**
- familie-home → /local/family.html (in HA Sidebar)
- wz-dashboard → /local/wz.html (in HA Sidebar)
- Erstellt via WebSocket API (panel_iframe wurde in HA 2025 entfernt)

**Garage Automation erstellt:**
- "Garagentor Warnung" → TTS nach 10 Min offen

**Nacht-Cron:**
- ID: 9cb6fc60-b8f8-4a3f-ad65-303c89c96aa8
- Täglich 02:00 CH Zeit → autonome HA Dashboard Improvements

### Offene Todos für Janis:
- SSH Key-only Auth (Janis entscheidet)
- Spotcast sp_dc Cookie konfigurieren
- Weitere Familienmitglieder für Presence-Tracking einrichten

## Session Fortsetzung 2026-03-23 22:02 UTC

### family.html — Komplette Überarbeitung heute

**Auth-Probleme gelöst:**
- OAuth "Invalid redirect URI" in Comet Browser → dynamische redirect_uri (location.origin+pathname) statt hardcoded
- iOS WKWebView iframe Touch-Bug → Button war nicht klickbar
- Kurze Sicherheitspanne: LLAT hardcoded im JS (PIN-Ansatz) → sofort revoked + entfernt
- Zurück zu echtem HA OAuth — sicher, 2FA-fähig

**Aktuelle Auth-Architektur:**
- Kein hardcoded Token, kein PIN
- HA OAuth mit dynamischer client_id/redirect_uri
- Parent.localStorage (hassTokens) für HA App iframe
- Token in localStorage 'wz4' gespeichert nach Login

**family.html Features:**
- 5 Tabs: Zuhause / Garage / Lichter / Wetter / ⚙️ System
- System-Tab: Neu laden, Cache leeren, PIN reset, Bug-Report, Logs
- PWA-fähig (Manifest, Apple Touch Icon)
- App-Icon: dunkler BG + oranges Haus (PNG generiert mit Python)

**Versionen deployed:**
- family.html: v2.1
- wz.html: v3.2
- nsscreen.html: v1.1
- nest-hub.html: v1.1
- Version immer im UI sichtbar (Footer/System-Tab)

**Security Audit abgeschlossen:**
- 14/14 Checks OK auf family.html
- LLAT sofort revoked nach Fehler
- Leaflet SRI-Hash zu wz.html hinzugefügt
- ha.apk aus /config/www/ gelöscht
- ipad.html: private IP entfernt (GitHub Pages)

**Janis Browser:** Comet (nicht Safari als Standard)
- Comet ersetzt Safari komplett auf iOS (inkl. PWA)
- OAuth funktioniert in Comet nach Fix
- PWA: Von Safari teilen → Homescreen; Icon-Update erfordert Neu-Hinzufügen

**HA Familie User:** Erstellt (id: 983880b1...) aber LLAT nicht mehr aktiv

**Git:** Commit 7 files, memory/2026-03-23.md erstellt
