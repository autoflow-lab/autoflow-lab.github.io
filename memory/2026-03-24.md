
## Nacht-Improvement Run 01:00 UTC

Zwei Verbesserungen in wz.html implementiert + deployed:

1. **Spotify Device-Chips**: Neue `buildSpotifyDeviceChips()` Funktion. Im Playlist-Panel werden jetzt alle verfügbaren Geräte als gestylte Chips angezeigt (📱/💻/📺/🔊 je nach Name). Aktives Gerät grün hervorgehoben. Klick = sofort `select_source`. Panel lädt jetzt immer neu beim Öffnen (nicht mehr gecacht) → Gerätestatus immer aktuell.

2. **Offline-Banner verbessert**: 
   - Zeigt jetzt **sofort bei 1. Fehler** wenn WS bereits down ist (vorher: erst ab 2. Fehler)  
   - Neues Design: Gradient-Banner mit Icon + Message + Countdown (`· Retry in Xs`)  
   - Countdown zählt live runter bis zum nächsten fetchStates-Retry  
   - Klick auf Banner → sofort retry (fetchStates)

Deploy: SSH (sudo tee via base64|gunzip) → /config/www/wz.html ✅
Commit: aa848b8

---

## Abend-Session 20:00–21:00 UTC

### family.html Versionen v2.7 → v3.2

**v2.7**: Touch UX Fixes
- `-webkit-user-select:none; -webkit-touch-callout:none` global → kein Text-Select beim Long-Press
- `touch-action:none` auf Slidern → kein Scroll beim Schieben
- Context-Menu global geblockt
- Long-Press Abbruch erst bei >6px Bewegung
- Caps fix: `light.schlafzimmer` + `light.deckenlampe` haben `xy` → caps:'color' (nicht 'ct')

**v2.8**: Govee LED Farbanpassung
- Sofa LED + Oberschrank → `caps:'rgb_nowhite'` eigene Palette ohne Weiß-Töne
- Warmweiß Swatch: RGB statt CT für alle Govees (konsistente Farbe)
- `COLORS_RGB` Array für rgb_nowhite Lights (12 reine Farben)
- Presets mit `rgb`-Feld für RGB-capable Lights

**v2.9**: Close-Button + Garage Fix
- Garagenstatus auf Home-Tab war immer "Lade..." → `updateGarage()` updated jetzt `garage-state-home`
- X-Button zum Schließen des Sheets (später durch Swipe ersetzt)
- Simulation erste Aktion: 8–25s statt 10–60s

**v3.0**: Swipe-to-Dismiss
- X-Button entfernt
- Sheet schließt sich durch Runter-Swipen (iOS-style)
- Velocity detection: schnelles Wischen schließt sofort
- `closeLdSheet()` globale Funktion mit smooth Animation

**v3.1**: Swipe-to-Dismiss v2 (Bewegungs-Tracking)
- `_ts` Koordinaten-Tracking beim touchstart
- Abbruch wenn >6px Bewegung (verhindert Scroll-Abbruch)
- Velocity per Frame berechnet

**v3.2**: Echte Hue Icons
- Alle SVG-Icons durch offizielle hass-hue-icons (arallsopp) ersetzt
- Icons: lightstrip, play_bar, iris, pendant, ceiling, bulb, wall_lamp, wardrobe, hallway, bathroom, outdoor
- CSS: `stroke:none; fill:currentColor` (Hue Icons sind filled, nicht stroked)
- `hass-hue-icons.js` (595KB) nach `/config/www/` deployed
- Als Lovelace Resource in HA registriert (ID: e5953d0e276e4fe1937d0530379ef4fc)
- Ab sofort in HA Dashboards: `hue:play-bar`, `hue:iris` etc. nutzbar

### Bekannte Issues (noch offen)
- wz.html Icons noch nicht auf Hue Icons umgestellt (nur family.html)
- wz.html Light-Mode hat noch hardcoded `#fff` in `.tagsum-stat .ts-val`
- WebSocket reconnect in wz.html noch nicht robuster gemacht

